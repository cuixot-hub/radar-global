name: Backtest oro y bitcoin (manual)
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow experimental desactivado. Resultado conservado en backtests/geopolitical_drawdown_ladder_summary.json."
