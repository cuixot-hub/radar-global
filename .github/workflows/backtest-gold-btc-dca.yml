name: Backtest oro y bitcoin (manual)
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow experimental desactivado. Resultado BTC recuperación semanal conservado en backtests/btc_recovery_filter_summary.json."
