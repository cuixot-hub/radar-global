name: Backtest oro y bitcoin (manual)
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow experimental desactivado para evitar ejecuciones automáticas y correos masivos. Resultados conservados en backtests/gold_btc_macro_filter_summary.json."
