name: Publicar Radar Global (PAUSADO)
on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Publicación automática pausada temporalmente para detener notificaciones masivas mientras se repara."
