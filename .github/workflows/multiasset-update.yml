name: Actualizar Semáforo Multi-Activo (PAUSADO)
on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Actualización automática pausada temporalmente para detener notificaciones masivas mientras se repara."
