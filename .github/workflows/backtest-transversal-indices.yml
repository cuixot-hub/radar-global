name: Backtest transversal índices (manual)
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow experimental desactivado para evitar ejecuciones automáticas y correos masivos."
